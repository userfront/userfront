/**
 * Just a message. Only used in development scenarios.
 *
 * @param {object} props
 * @param {string} props.text
 */
const Message = ({ text }) => {
  return <p>{text}</p>;
};

export default Message;
